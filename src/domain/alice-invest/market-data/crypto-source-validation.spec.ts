import { describe, expect, it } from 'vitest'
import { validateCryptoReadOnlySource } from './crypto-source-validation.js'
const now=new Date('2026-07-16T12:00:10Z'); const obs=(symbol:string)=>({source:'fixture',symbol,sourceTimestamp:'2026-07-16T12:00:00.000Z',receivedAt:now.toISOString(),capability:'realtime' as const,volume:'1'})
describe('crypto read-only evidence',()=>{it('requires fresh spot-only observations and reconnect',()=>expect(validateCryptoReadOnlySource({observations:[obs('BTC/USDT'),obs('ETH/USDT')],reconnectSucceeded:true,maxAgeSeconds:30,now,spotReadOnly:true,withdrawalsEnabled:false,marginEnabled:false,futuresEnabled:false,leverageEnabled:false})).toMatchObject({capability:'realtime',signalsAllowed:true}));it('rejects a source with withdrawal capability',()=>expect(validateCryptoReadOnlySource({observations:[obs('BTC/USDT')],reconnectSucceeded:false,maxAgeSeconds:30,now,spotReadOnly:true,withdrawalsEnabled:true,marginEnabled:false,futuresEnabled:false,leverageEnabled:false}).signalsAllowed).toBe(false))})
