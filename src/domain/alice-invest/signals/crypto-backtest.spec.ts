import { describe,expect,it } from 'vitest'
import { runCryptoSpotBacktest,type CryptoBacktestFixture } from './crypto-backtest.js'
const fixture:CryptoBacktestFixture={version:1,source:'crypto-versioned-fixture-2026-07',symbol:'BTC/USDT',bars:[['2026-07-18T00:00:00.000Z','100'],['2026-07-18T01:00:00.000Z','101'],['2026-07-18T02:00:00.000Z','102'],['2026-07-18T03:00:00.000Z','103'],['2026-07-18T04:00:00.000Z','104'],['2026-07-18T05:00:00.000Z','105']].map(([timestamp,close])=>({timestamp,open:close,high:new Number(close).valueOf()+3+'',low:new Number(close).valueOf()-1+'',close,volume:'1000'}))}
const config={feeBps:'10',slippageBps:'5'}
describe('crypto spot backtest',()=>{it('is deterministic across a weekend and records spot costs/slippage',()=>expect(runCryptoSpotBacktest(fixture,config)).toMatchObject([{signalAt:'2026-07-18T04:00:00.000Z',entryAt:'2026-07-18T05:00:00.000Z',exitReason:'target'}]));it('does not use a future candle to create the signal',()=>{const changed={...fixture,bars:fixture.bars.map((bar,index)=>index===5?{...bar,high:'999'}:bar)};expect(runCryptoSpotBacktest(changed,config)[0].signalAt).toBe(runCryptoSpotBacktest(fixture,config)[0].signalAt)});it('rejects an invalid fixture timestamp',()=>expect(runCryptoSpotBacktest({...fixture,bars:[{...fixture.bars[0],timestamp:'nope'},...fixture.bars.slice(1)]},config)).toEqual([]))})
