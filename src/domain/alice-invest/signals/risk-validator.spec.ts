import { describe,expect,it } from 'vitest'
import { validateSignalRisk } from './risk-validator.js'
const now=new Date('2026-07-16T12:00:00Z'); const candidate={strategyId:'x',strategyVersion:'1',symbol:'BTC',observations:[{symbol:'BTC',source:'x',sourceTimestamp:'2026-07-16T11:59:50.000Z',receivedAt:now.toISOString(),capability:'realtime' as const,close:'100'}],targetPrice:'104',stopPrice:'98',validUntil:'2026-07-16T13:00:00.000Z',riskNotes:[],status:'eligible' as const}; const valid=(x={})=>validateSignalRisk({candidate,now,maxAgeSeconds:30,spreadPct:'0.1',maxSpreadPct:'1',liquidityBRL:'1000',minimumLiquidityBRL:'100',cooldownActive:false,positionLimitReached:false,derivatives:false,margin:false,leverage:false,executionRequested:false,minimumRewardRisk:'1',...x})
describe('risk validator',()=>{it('allows compliant informational candidate',()=>expect(valid()).toEqual({allowed:true,reasons:[]}));it('rejects deterministic violations',()=>expect(valid({spreadPct:'2',cooldownActive:true,derivatives:true,executionRequested:true})).toMatchObject({allowed:false,reasons:expect.arrayContaining(['spread','cooldown','derivative_or_leverage','execution_requested'])}))})
