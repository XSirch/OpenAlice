import{describe,expect,it}from'vitest';import{assessCapabilityReadiness}from'./core.js';const ok={sourceFresh:true,riskAllowed:true,formatterReady:true,executionEnabled:false as const,telegramOwnerReady:true,recoveryReady:true};describe('readiness core',()=>{it('caps every eligible capability at paper alerts',()=>expect(assessCapabilityReadiness(ok).state).toBe('paper_alerts'));it('blocks one capability without affecting another',()=>{expect(assessCapabilityReadiness({...ok,sourceFresh:false}).state).toBe('research_only');expect(assessCapabilityReadiness(ok).state).toBe('paper_alerts')})})
