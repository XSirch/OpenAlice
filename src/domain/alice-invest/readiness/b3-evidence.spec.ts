import{describe,expect,it}from'vitest';import{evaluateB3Evidence}from'./b3-evidence.js';describe('B3 evidence',()=>{it('fails closed without realtime evidence',()=>expect(evaluateB3Evidence({events:[],sourceRealtime:false,sourceFresh:false,outages:1,stale:2,duplicates:3})).toMatchObject({readiness:'research_only',outages:1,stale:2,duplicates:3}));it('permits paper alerts only with complete evidence',()=>expect(evaluateB3Evidence({events:[],sourceRealtime:true,sourceFresh:true,outages:0,stale:0,duplicates:0}).readiness).toBe('paper_alerts'))})
